[project]
name = "arcal-busmon"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.109",
    "uvicorn[standard]>=0.27",
    "websockets>=12.0",
]

[project.scripts]
busmon = "busmon.app:main"
